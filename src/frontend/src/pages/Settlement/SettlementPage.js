import { Card } from "antd";

export default function SettlementPage() {
  return (
    <Card title="Settlement">
      Settlement UI will be implemented after overtime settlement endpoint flow
      is confirmed.
    </Card>
  );
}
